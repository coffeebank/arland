---
---
{
    serverName: "{{ site.data.server.serverName }}",
    serverIP: "{{ site.data.server.serverIP }}",
    serverPort: "{{ site.data.server.serverPort | escape }}",
    compatibleVersions: "{{ site.data.server.compatibleVersions | escape }}",
    shownotes: "{{ site.data.server.shownotes }}",
    notes: "{{ site.data.server.notes | escape }}"
}